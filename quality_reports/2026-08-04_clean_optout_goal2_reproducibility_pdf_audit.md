# Independent Reproducibility and PDF Audit — Goal 2

**Date:** 2026-08-04
**Reviewed commit:** 1b8bda6fb6906391c65fb6425b781c123d5948be
**Review mode:** independent, read-only audit from a `git archive` snapshot
**Rmd SHA-256:** 131cc2356cd6318211fdbb9304ac8d7c8a99356837b6e71097011c36ae9c270d
**Versioned PDF SHA-256:** a7d36d5a1fb2d15ba0e40509ad846fbf001b5960232fcbf011d5b32bec298bdf

## Verdict

**PASS — no substantive reservation.**

The manuscript is reproducible in its declared YAML/bookdown format. The
versioned PDF is textually and visually identical to a clean independent
render once volatile creation metadata are ignored. All numerical and textual
migration checks pass, and the complete PDF has no clipping or other visible
layout defect.

## Reproduction results

| Check | Result |
|---|---:|
| YAML/bookdown render | PASS |
| Six clean-baseline R validators | 96/96 PASS |
| Goal 2 migration validator | 14/14 PASS |
| **Combined automated checks** | **110/110 PASS** |
| Six regenerated analytical CSVs | Byte-identical |
| Extracted layout text | Hash-identical |
| Extracted raw text | Hash-identical |
| Thirty-two regenerated page rasters | Pixel-identical |

The clean re-render produced a different raw PDF hash because its creation
timestamp changed. This is a metadata-only difference:

- clean-render PDF SHA-256:
  `ecafd662aedeae8f2ebdfd6e7ae58a7684e421dcdb644e31a15f9117a0f69e68`;
- layout-text SHA-256 for both PDFs:
  `ed41352ac8c437d11b9df5545e4b196dd9837428133f4d68414022f021891d8e`;
- raw-text SHA-256 for both PDFs:
  `d5010839fd5c6f5d7d42af01531d1321213036ae6e5c6e66be18931e72d99bb5`.

The six analytical CSV outputs reproduced byte for byte. The migration CSV's
only clean-snapshot delta was the newly rendered PDF hash recorded by design.

## PDF audit

The PDF has 32 Letter pages: 28 portrait pages and four landscape table
pages. Poppler successfully parsed, extracted, and rasterized every page. The
auditor verified:

- six embedded, subsetted fonts with Unicode mappings;
- seven numbered tables and one numbered figure;
- 49 sequentially numbered equations;
- 32 headings and 12 formal environments;
- no clipping, overlap, missing glyph, broken table, orphaned caption, or
  illegible content.

The first audit pass found one genuine defect in the prior candidate: the TSV
source path on physical page 16 crossed the media box. Commit
1b8bda6fb6906391c65fb6425b781c123d5948be placed the path on a centered
standalone line. In the repaired PDF it occupies x=180--432 points on a
612-point page and has a 147-pixel right raster margin. All pages now have
positive visual margins. Relative to the prior candidate, only page 16
changed; the other 31 pages are pixel-identical.

## References and migration scope

- 65 source labels are unique;
- all cross-references resolve, including two table labels generated by
  `kable`;
- 14 cited keys resolve to 31 unique bibliography entries, with no duplicate
  entries;
- excluded historical architectures are absent from operative results and
  appear only where the manuscript delimits scope.

## Nonblocking environment notes

The audit observed locale warnings and the expected absence of `.git` inside
the archive snapshot. `qpdf` was unavailable, but Poppler completed metadata,
text, font, page, and raster inspection. These limitations do not affect the
verdict.
